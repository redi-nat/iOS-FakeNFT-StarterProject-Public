import Foundation

/// Модель валюты для оплаты
/// Используется для выбора валюты при оплате заказа
struct Currency: Decodable {
    let id: String
    let title: String        // полное название валюты (например, "Ethereum")
    let name: String         // сокращенное название (например, "ETH")
    let image: URL?         // логотип валюты
    
    // Если структура API отличается, нужно будет скорректировать поля
    // Например, если image приходит как строка или в другом формате
}

